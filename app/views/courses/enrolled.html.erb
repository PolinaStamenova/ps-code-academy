<div class='container mx-auto p-4 w-3/5 mt-10'>
  <div class='text-balance'>
    <h1 class='text-lg font-bold'> Your enrolled courses </h1>
    <%# --- Courses --- %>
    <%= render partial: '/courses/courses_list', locals: { courses: @courses } %>
  </div>
</div>
